# Please do not edit this file manually
# It should be added in your version-control system (e.g., Git)
#
# This exists so the migrations directory is present before the first model is
# added. Without it `prisma migrate deploy` fails with "Could not find a
# migrations directory" and CI has nothing to run.
provider = "postgresql"
